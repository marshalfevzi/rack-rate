{
  "name": "@rack-rate/data-cli",
  "version": "2.0.0",
  "private": true,
  "description": "Gather, normalize, validate and derive benchmark + plan data. Network and filesystem live only here.",
  "bin": {
    "rack-rate-data": "./src/main.ts"
  },
  "type": "module",
  "scripts": {
    "cli": "bun run src/main.ts"
  },
  "dependencies": {
    "@rack-rate/core": "workspace:*",
    "zod": "^4.6.4"
  }
}
