---
id: APP-601
title: src/lib/prefs.ts — typed nanostores persistent stores under one
description: "src/lib/prefs.ts — typed nanostores persistent stores under one versioned key namespace, rack-rate:prefs:v1, holding { ignoredModels, ignoredPlans, paidPlans, vendor, currency, budgetCeiling, weights, benchmarkFilters }. SSR-safe (no..."
status: todo
milestone: M6
pre: []
kind: feature
blocked_by: null
created: 2026-09-17
completed: null
---

## Scope

`src/lib/prefs.ts` — typed `nanostores` persistent stores under one versioned key namespace, `rack-rate:prefs:v1`, holding `{ ignoredModels, ignoredPlans, paidPlans, vendor, currency, budgetCeiling,   weights, benchmarkFilters }`. SSR-safe (no `localStorage` read during frontmatter), corrupt values fall back to defaults, cross-tab sync on. Bumping the version prefix is how a schema change migrates: read the old key once, transform, write the new one, never crash on the old shape. Every read passes the same zod schema any other trust boundary uses, so a hand-edited or version-stale payload cannot crash a page; a key that fails validation falls back to that key's default rather than being dropped silently. The file does not exist before this task and Stage 5 deliberately shipped without it.

## Acceptance criteria

- [ ] The requirement text in `## Scope` is satisfied and verified; stage acceptance: `docs/pm/M6/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 6 task 6.1 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M6/README.md`. The task has not been enriched or rewritten.
