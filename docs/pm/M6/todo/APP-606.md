---
id: APP-606
title: Export/import preferences as JSON (a single file, versioned), plus a
description: Export/import preferences as JSON (a single file, versioned), plus a 'reset all preferences' action.
status: todo
milestone: M6
pre: []
kind: feature
blocked_by: null
created: 2026-09-17
completed: null
---

## Scope

Export/import preferences as JSON (a single file, versioned), plus a "reset all preferences" action.

## Acceptance criteria

- [ ] The requirement text in `## Scope` is satisfied and verified; stage acceptance: `docs/pm/M6/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 6 task 6.6 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M6/README.md`. The task has not been enriched or rewritten.
