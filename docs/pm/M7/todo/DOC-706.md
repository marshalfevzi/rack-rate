---
id: DOC-706
title: Update the plan record so every stage is ticked or explicitly
description: Update the plan record so every stage is ticked or explicitly carried, update AGENTS.md if any command changed, and write the final ARCHITECTURE.md.
status: todo
milestone: M7
pre: []
kind: docs
blocked_by: null
created: 2026-09-17
completed: null
---
## Scope

Update the plan record so every stage is ticked or explicitly
  carried (`docs/pm/plan.yml` is generated from the source documents, so a stage
  tick follows the milestone frontmatter rather than a hand edit of the index),
  update `AGENTS.md` if any command changed, and write the final
  `ARCHITECTURE.md`.

## Acceptance criteria

- [ ] The requirement text in `## Scope` is satisfied and verified; stage acceptance: `docs/pm/M7/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 7 task 7.6 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M7/README.md`. The task has not been enriched or rewritten.
