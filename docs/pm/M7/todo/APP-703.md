---
id: APP-703
title: "Data freshness without breaking determinism: a scheduled workflow that"
description: "Data freshness without breaking determinism: a scheduled workflow that runs the fetchers, and opens a pull request instead of pushing directly when data/*.json changes, so every data movement is reviewable and the deployed site is al..."
status: todo
milestone: M7
pre: []
kind: chore
blocked_by: null
created: 2026-09-17
completed: null
---

## Scope

Data freshness without breaking determinism: a scheduled workflow that runs the fetchers, and **opens a pull request** instead of pushing directly when `data/*.json` changes, so every data movement is reviewable and the deployed site is always built from a committed, validated snapshot.

## Acceptance criteria

- [ ] The requirement text in `## Scope` is satisfied and verified; stage acceptance: `docs/pm/M7/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 7 task 7.3 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M7/README.md`. The task has not been enriched or rewritten.
