---
id: APP-701
title: .github/workflows/deploy.yml — Bun setup with a pinned version,
description: ".github/workflows/deploy.yml — Bun setup with a pinned version, bun install --frozen-lockfile, bun run build, withastro/action upload, actions/deploy-pages deploy, correct permissions: block, concurrency group so overlapping pushes c..."
status: todo
milestone: M7
pre: []
kind: chore
blocked_by: null
created: 2026-09-17
completed: null
---

## Scope

`.github/workflows/deploy.yml` — Bun setup with a pinned version, `bun install --frozen-lockfile`, `bun run build`, `withastro/action` upload, `actions/deploy-pages` deploy, correct `permissions:` block, concurrency group so overlapping pushes cancel.

## Acceptance criteria

- [ ] The requirement text in `## Scope` is satisfied and verified; stage acceptance: `docs/pm/M7/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 7 task 7.1 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M7/README.md`. The task has not been enriched or rewritten.
