---
id: DOC-707
title: "Final verification: clean clone → bun install → bun run build →"
description: "Final verification: clean clone → bun install → bun run build → deployed URL loads, every route reachable, chart interactions work on a phone-sized viewport, and the sources page lists every upstream plus the attribution block."
status: todo
milestone: M7
pre: []
kind: chore
blocked_by: null
created: 2026-09-17
completed: null
---
## Scope

Final verification: clean clone → `bun install` → `bun run build` →
  deployed URL loads, every route reachable, chart interactions work on a
  phone-sized viewport, and the sources page lists every upstream plus the
  attribution block.

## Acceptance criteria

- [ ] The requirement text in `## Scope` is satisfied and verified; stage acceptance: `docs/pm/M7/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 7 task 7.7 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M7/README.md`. The task has not been enriched or rewritten.
