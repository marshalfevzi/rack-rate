---
id: APP-303
title: "Layouts, link helpers, two-scheme token set"
description: "`Base.astro` and `Page.astro` layouts with head/meta, canonical URL from `Astro.site` + `base`, OG tags, skip-link, sticky nav and the attribution footer, with every internal link going through one `href()` helper."
status: done
milestone: M3
pre: []
kind: feature
blocked_by: null
created: 2026-09-14
completed: 2026-09-14
---
## Scope

`src/layouts/Base.astro` + `Page.astro`: head/meta, canonical URL
built from `Astro.site` + `base`, OG tags, skip-link, sticky nav, footer with
the attribution block. Every internal link goes through one `href()` helper
so `base` is applied consistently.

## Acceptance criteria

- [x] The requirement text in `## Scope` is satisfied; the landing evidence is the stage session record in `## Session` and the stage acceptance at `docs/pm/M3/README.md`.

## Notes

Split verbatim from the frozen pre-PM stage record `docs/history/stages-3.md` §`Stage 3 — Static build framework` task 3.3 by the PM history parse on 2026-09-17. The canonical id is `APP-303`; the stage's label `3.3` is its own numbering in force at the time, not a task name. The stage's acceptance and handover contract live at `docs/pm/M3/README.md`. The task text has not been enriched or rewritten.

## Session

The stage logged this work inside its stage-level entry `### 2026-09-14 — Stage 3.3 + 3.3b: layouts, link helpers, two-scheme token set`; that entry is preserved verbatim in `docs/pm/M3/README.md` § Stage session log and in `docs/history/stages-3.md`. No per-task session entry exists.
