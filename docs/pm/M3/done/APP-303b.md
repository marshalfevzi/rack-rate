---
id: APP-303b
title: "Visual design pass"
description: "Visual design pass replacing the predecessor's accidental signals with a type scale and spacing rhythm, one accent per semantic role, one motion duration and easing, and a two-scheme palette recorded in `docs/architecture.md`."
status: done
milestone: M3
pre: []
kind: design
blocked_by: null
created: 2026-09-14
completed: 2026-09-14
---

## Scope

**Visual design pass** — the old site's problems are enumerated, so fix them deliberately rather than by taste alone. Replace the accidental signals the predecessor accumulated: 3D glossy ball chart markers, amber used for everything, monospace used for everything, dashed-rule noise, four competing animation durations, an emoji-based empty state, and a dead analytics snippet. Deliverables: a type scale and spacing rhythm (three or four sizes, one spacing unit), one accent per semantic role (adjusted / measured / API), one motion duration and one easing with `prefers-reduced-motion` honoured, and a two-scheme palette. Record the tokens and the reasoning in `docs/architecture.md` so Stage 4 does not re-invent them.

## Acceptance criteria

- [x] The requirement text in `## Scope` is satisfied; the landing evidence is the stage session record in `## Session` and the stage acceptance at `docs/pm/M3/README.md`.

## Notes

Split verbatim from the frozen pre-PM stage record `docs/history/stages-3.md` §`Stage 3 — Static build framework` task 3.3b by the PM history parse on 2026-09-17. The canonical id is `APP-303b`; the stage's label `3.3b` is its own numbering in force at the time, not a task name. The stage's acceptance and handover contract live at `docs/pm/M3/README.md`. The task text has not been enriched or rewritten.

## Session

The stage logged this work inside its stage-level entry `### 2026-09-14 — Stage 3.3 + 3.3b: layouts, link helpers, two-scheme token set`; that entry is preserved verbatim in `docs/pm/M3/README.md` § Stage session log and in `docs/history/stages-3.md`. No per-task session entry exists.
