---
id: UI-512
title: "`/start` — the selection console at full width, layout only"
description: "/start — the selection console at full width, layout only. Files: apps/site/src/pages/start.astro. Work: give /start its lane, the full-width selection console layout from docs/design/surfaces.md, the readout it will write into, and..."
status: todo
milestone: M9
pre: [UI-902]
kind: design
blocked_by: null
created: 2026-09-17
completed: null
---

## Scope

**`/start` — the selection console at full width, layout only.** **Files:** `apps/site/src/pages/start.astro`. **Work:** give `/start` its lane, the full-width selection console layout from `docs/design/surfaces.md`, the readout it will write into, and the empty and error grammar — no flow logic. It renders the three step headings, the selection surfaces and the recommendation block's frame with an honest `No committed rows for this view.`-class empty state, so the page is legible and keyboard-traversable before its behaviour exists. **Done when** `/start` renders under the new shell at 1440px, 768px and 360px with no horizontal scroll, contains no selection logic and no persisted state, and does not claim a saved preference.

## Acceptance criteria

- [ ] Every clause of the task's **Done when** in `## Scope` holds, verified from a built preview (`bun run build` then `bun run preview`); stage acceptance: `docs/pm/M9/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 5 task 5.12 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M9/README.md`; the task moved from M5 to M9 on 2026-09-18, because its route is rebuilt in the replacement visual world rather than converted into the Console Listing. The task has not been enriched or rewritten.
