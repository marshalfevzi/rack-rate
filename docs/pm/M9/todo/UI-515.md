---
id: UI-515
title: Re-derive the design record from the built world
description: "Re-derive the design record from the built world. Files: DESIGN.md, docs/design/surfaces.md, apps/site/.impeccable/surfaces/apps-site-src-pages-index-astro.md, ARCHITECTURE.md. Work: re-derive each document from the shipped cod..."
status: todo
milestone: M9
pre: [UI-514]
kind: design
blocked_by: null
created: 2026-09-17
completed: null
---

## Scope

**Re-derive the design record from the built world.** **Files:** `DESIGN.md`, `docs/design/surfaces.md`, `apps/site/.impeccable/surfaces/apps-site-src-pages-index-astro.md`, `ARCHITECTURE.md`. **Work:** re-derive each document from the shipped code rather than editing it into agreement — read the emitted `@theme` block, the shell's measured geometry and the components actually on disk, then correct the documents to match. The impeccable `document` pass (`.claude/skills/impeccable/`) is this repository's tool for that and is the expected route; a session without it must still produce the same result by hand, because **the deliverable is the corrected documents, not the pass**. Resolve `DESIGN.md`'s readout-docking sentence against the docking 5.4 shipped and carry the same docking into the surface brief; give `ARCHITECTURE.md` its Console Listing section — both schemes with their audited ratios, the shell metrics, the primitive inventory, the chart grammar and the readout contract — and delete its superseded Stage 4 token prose rather than leaving it beside the new section. **Done when** every value, token, metric and docking in `DESIGN.md`, `docs/design/surfaces.md`, the surface brief and `ARCHITECTURE.md` is one the built site actually produces, checked by reading the emitted CSS and the measured geometry rather than by reading another document.

## Acceptance criteria

- [ ] Every clause of the task's **Done when** in `## Scope` holds, verified from a built preview (`bun run build` then `bun run preview`); stage acceptance: `docs/pm/M9/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 5 task 5.15 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M9/README.md`; the task moved from M5 to M9 on 2026-09-18, because its route is rebuilt in the replacement visual world rather than converted into the Console Listing. The task has not been enriched or rewritten.
