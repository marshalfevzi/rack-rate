---
id: DEC-2026-09-14-005
date: 2026-09-14
status: accepted
milestone: M1
tasks:
  - DATA-104b
  - COR-105
supersedes: null
---
# Clean cutover from the Python pipeline, with the legacy output frozen as the parity oracle first

## Context

Task 1.5 removes the superseded stack. Its task text reads: "Delete the superseded stack: `scripts/*.py`, `site/template.html`, `site/index.html`, `site/fonts/`, `vercel.json`, and Vercel Web Analytics (dead once the site is off Vercel). Retire the generated `data/derived.*` once Stage 2 regenerates them." The recorded owner decision for that task is: "Owner decision: clean cutover, not a parallel track — but only after 1.4b has frozen the legacy output as a fixture." Task 1.4b is the precondition: "**Freeze the legacy pipeline's output as a golden fixture before deleting it.**" It copies `data/derived.json` (178 pairs, 28 best routes, cross-check median 1.601 over n=11) and `data/derived.csv` to `data/fixtures/legacy-derived.json` / `.csv`, commits them, and records the producing commands and input hashes, with the ordering rule "Do this **before** 1.5 runs."

## Decision

Clean cutover, not a parallel track — but only after 1.4b has frozen the legacy output as a fixture. Task 1.4b runs first; task 1.5 then deletes `scripts/*.py`, `site/template.html`, `site/index.html`, `site/fonts/`, `vercel.json`, Vercel Web Analytics, and retires the generated `data/derived.*` once Stage 2 regenerates them.

## Consequences

`docs/history/README.md` §`Frozen fixtures and gates` records the frozen hashes: `data/fixtures/legacy-derived.json` sha256 `803cef427a6af2f32ade594518b1806a49bb21fea90cc80fc52684f10bab9ded`, and `data/fixtures/legacy-derived.csv` sha256 `6759e509df222e07771b25086a22061d5f701da2da84793fa1ea5af3da98df34`. "These are the legacy pipeline's frozen output and the port-parity oracle asserted in `bun test` against `data/fixtures/legacy-inputs.{models,plans}.json`."
