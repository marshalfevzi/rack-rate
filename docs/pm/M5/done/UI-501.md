---
id: UI-501
title: Token foundation and scheme inversion
description: "Token foundation and scheme inversion. Files: apps/site/src/styles/global.css. Work: replace the nine incumbent tokens (canvas, panel, rule, ink, dim, adjusted, measured, api, api-ink) with the frozen ten, adding panel-2, rule-strong..."
status: done
milestone: M5
pre: []
kind: design
blocked_by: null
created: 2026-09-17
completed: 2026-09-17
---

## Scope

**Token foundation and scheme inversion.**
**Files:** `apps/site/src/styles/global.css`.
**Work:** replace the nine incumbent tokens (`canvas`, `panel`, `rule`, `ink`,
`dim`, `adjusted`, `measured`, `api`, `api-ink`) with the frozen ten, adding
`panel-2`, `rule-strong`, `faint`, `signal` and `on-signal`; replace the
four-step type scale with the frozen six steps at fixed pixel line-heights,
with the Sans and Mono stacks named per role; keep `--spacing: 0.25rem` as the
single spacing unit; replace the ink `:focus-visible` ring with a 2px
`--color-signal` ring offset 2px; theme the browser surfaces DESIGN.md names
(selection plate, caret, scrollbar track and thumb, underline offset); make the
light scheme a value-only inversion of the same ten tokens, including the
`#FFB020` signal plate against `--color-on-signal`; keep one duration, one
easing and the reduced-motion override.
**Done when** every token in `DESIGN.md` exists in `@theme`, all four retired
accent tokens and the utilities they generated are gone, and a contrast
recomputation for both schemes against canvas, panel **and** panel-2 clears the
floors in `DESIGN.md`, with `--color-faint` recorded as label-only.

## Acceptance criteria

- [ ] Every clause of the task's **Done when** in `## Scope` holds, verified from a built preview (`bun run build` then `bun run preview`); stage acceptance: `docs/pm/M5/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 5 task 5.1 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M5/README.md`. The task has not been enriched or rewritten.

## Session

Landed the Console Listing token foundation in `apps/site/src/styles/global.css` and recorded it in the `## Design tokens` section of `ARCHITECTURE.md`; no other file changed.

Shipped: `--color-*: initial` plus the ten frozen `--color-*` tokens from `DESIGN.md` (`canvas`, `panel`, `panel-2`, `rule`, `rule-strong`, `ink`, `dim`, `faint`, `signal`, `on-signal`), with the four retired accents (`adjusted`, `measured`, `api`, `api-ink`) deleted and no alias or shim left; the six frozen `--text-*` steps at DESIGN.md's pixel line-heights (16/19/24/20/25/36) with micro tracking `0.08em`; `--spacing: 0.25rem`; one duration (150ms), one easing and the `prefers-reduced-motion` block retained; `:focus-visible` is now a 2px `--color-signal` ring offset 2px; `::selection`, `caret-color`, `scrollbar-color` with the WebKit scrollbar pseudos, and `text-underline-offset: 2px` theme the browser surfaces; the light scheme re-declares the same ten names as values only. `--signal-plate` is the one derived property (`var(--color-signal)` in dark, `#ffb020` in light), and its reason is measured: in light `--color-on-signal` on the mark `--color-signal` is 2.79:1 while on the `#ffb020` plate it is 9.84:1.

Evidence, taken from a built preview (`bun run build` then `bun run preview`, never `astro dev`) at 1440x900 in both schemes with `getComputedStyle`:

- all ten `--color-*` resolve to DESIGN.md's values; `--signal-plate` is `#ffb020` in both; `colorScheme` flips; `body` computes 15px/24px.
- focus ring `2px solid` with `2px` offset in the scheme's signal, measured on a real Tab keystroke; `::selection` paints `rgb(255,176,32)` with the scheme's `--color-on-signal` text; caret `rgb(255,176,32)` / `rgb(143,78,0)`; `scrollbar-color` `rgb(58,64,72) rgb(23,26,30)` / `rgb(183,188,195) rgb(237,238,240)`; `text-underline-offset` `2px`.
- the six steps compute 11px/16px/0.88px/500, 13px/19px/400, 15px/24px/400, 14px/20px/400, 20px/25px/600 and 32px/36px/-0.64px/600.
- the emitted stylesheet's unique `--color-*` name set is exactly the ten frozen names: no retired accent name, no utility class for them and no default-palette variable remain.
- contrast recomputed twice (implementer, then independently by `pm-verifier`) with WCAG 2.x relative luminance, reproducing DESIGN.md's audited ratios exactly: every text pair clears 4.5:1 against canvas, panel and panel-2 in both schemes; the tightest is `--color-faint` on panel-2 at 4.81:1 dark and 4.65:1 light, recorded as label-only.

Gates: `bun run check` exit 0 (tsc, oxlint, markdown-lint 0 errors in 161 files, oxfmt clean, `astro check` 0 errors/0 warnings/0 hints); `bun test` 153 pass / 0 fail; `bun run data:check` exit 0 with `data/derived.json` still `7425a331008fe0a1281a6d4f0bf4f350987f656cd135141a1ac69ef3f2317348`; `bun run build` exit 0. `dist/og.png` is 1200x630, sha256 `0ca25727742dfa2f431d6d44e5a4808dbadd8b362a4d30064a994249549659db`; its palette change is UI-502's. A comment-only correction on `--color-signal` was made after verification and proved inert: the rebuilt stylesheet hashes identically to the verified one.

Independent verification: `pm-verifier`, read-only, nine checks, pass on all nine, no defects.

Known transients left untouched because their files belong to later tasks: the retired-utility callers (`components/{Badge,CostBasisChip,SourceLink}.astro`, `pages/{compare,method,sources,models/[slug]}.astro`) now fall back to inherited ink/dim, and `lib/charts/theme.ts` still reads the four retired accents at runtime, so chart mounts fail and the pages print "The chart could not be drawn" until UI-509 re-themes them. Tailwind prunes unconsumed theme variables, so `--color-faint` (dark), `--text-micro` and `--text-data` are absent from the emitted theme `:root` until UI-503/505/509 consume them, exactly as the stage-4 record already documents for `--ease-standard`.
