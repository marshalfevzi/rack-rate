---
id: UI-504
title: The cursor readout
description: "The cursor readout. Files: new apps/site/src/components/Readout.astro and apps/site/src/lib/readout.ts; a mount point in Base.astro; fixed-line geometry in global.css. Work: one readout element per page, aria-live='polite', printing..."
status: done
milestone: M5
pre: []
kind: design
blocked_by: null
created: 2026-09-17
completed: 2026-09-17
---

## Scope

**The cursor readout.**
**Files:** new `apps/site/src/components/Readout.astro` and
`apps/site/src/lib/readout.ts`; a mount point in `Base.astro`; fixed-line
geometry in `global.css`.
**Work:** one readout element per page, `aria-live="polite"`, printing
`VALUE · BASIS · CONFIDENCE · SOURCE · RETRIEVED` in that fixed order and never
in another; pre-filled from the page's own headline figure so it is never blank
and never a hint string; pointer hover, keyboard focus and touch focus all
route through one update path; a missing value prints `—` with the
`known_gaps` reason beside it. **This task resolves the one contradiction
between the two design documents:** `DESIGN.md`'s "Readout behavior" docks the
readout in the right column of `/`'s split console, while
`docs/design/surfaces.md` docks it as the fixed 28px line at the viewport
bottom at ≥768px and directly under the band below that. Ship
`surfaces.md`'s docking — one fixed 28px line, page body reserving 28px of
bottom padding so it never covers the footer index, and the same element inline
under the band below 768px — record the choice in `ARCHITECTURE.md`, and
hand the `DESIGN.md` sentence to 5.15.
**Done when** the readout is one element, never blank, updated identically by
pointer and by keyboard, not inserted as a repeated tab stop, and unchanged
under `prefers-reduced-motion`.

## Acceptance criteria

- [ ] Every clause of the task's **Done when** in `## Scope` holds, verified from a built preview (`bun run build` then `bun run preview`); stage acceptance: `docs/pm/M5/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 5 task 5.4 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M5/README.md`. The task has not been enriched or rewritten.

## Session

Implemented the one document-level cursor readout. New `apps/site/src/lib/readout.ts` (pure five-field contract, attribute reader, `readoutAttributes`, per-route `defaultReadout`, `shellStampReadouts`) and `apps/site/src/components/Readout.astro` (the single `[data-readout-line]`: `role="status"`, `aria-live="polite"`, `aria-atomic="true"`, no tab stop, two delegated `document` listeners — `focusin` and `pointerover` — through one `apply()`); mounted once in `Base.astro` between the band and the shell grid with a `Page` prop seam; the band's three stamps carry the readout carrier; `.readout-line` is inline under the band below 768px and a fixed 28px line with a 28px body reserve at ≥768px. Only `readout.ts`, `readout.test.ts`, `Readout.astro`, `Base.astro`, `Page.astro`, `StatusBand.astro`, `global.css`, `url.ts` and `ARCHITECTURE.md` changed.

Docking: `docs/design/surfaces.md` over `DESIGN.md`'s readout-docking sentence, recorded in `ARCHITECTURE.md` and handed to UI-515; decision `2026-09-17-002`. `docs/design/surfaces.md:326` supplies `/start`'s empty-state SOURCE and `:399` the 404's literal `Page not found · n/a · n/a · rack-rate · n/a` record; `Known gap:` stays reserved for a data gap.

Verification from the built preview (`bun run build` then `bun run preview`, never `astro dev`): all eleven routes render exactly one readout, all five fields in the frozen order, none blank, and every pre-fill matches the committed record (including a non-leading slug); at 768/1024/1440 the line is `position: fixed`, full bleed, `height: 28px`, `body` padding-bottom `28px`, inner row 28px, `scrollWidth === clientWidth`, footer bottom exactly at the line top at end of scroll, widest record 659.42px of the 704px content box; at 390/360 it is static at y=32 directly under the band with `body` padding-bottom `0px` and no horizontal overflow; hovering the band's `AA` anchor and tabbing to it print byte-identical records, hover reaches a different record and persists, a full tab trace never enters the readout, and `prefers-reduced-motion: reduce` leaves the record identical with the readout's computed transition duration equal to the band's (`0s` normal, the document-wide `1e-05s` under reduce).

Gates: `bun run check` exit 0 (typecheck, oxlint, markdown lint, `oxfmt --check`, `astro check` 0 errors); `bun test` 157 pass / 0 fail; `bun run data:check` exit 0 with `data/derived.json` still `7425a331008fe0a1281a6d4f0bf4f350987f656cd135141a1ac69ef3f2317348`; `bun run build` exit 0 (53 pages, `og.png` 1200×630). Independent `pm-verifier` pass returned finish with no defects at any severity. One approved deviation: `apps/site/src/lib/url.ts` normalises an absent Bun `import.meta.env.BASE_URL` so the pure helpers stay importable under `bun test` without changing the Astro build path.
