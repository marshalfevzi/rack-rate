---
id: UI-506
title: "`/` — the split console"
description: "/ — the split console. Files: apps/site/src/pages/index.astro. Work: recompose the entry as the split console: the left selection column is plan → models → tasks per month, pre-filled with a real committed plan and its measured-again..."
status: done
milestone: M5
pre: []
kind: design
blocked_by: null
created: 2026-09-17
completed: 2026-09-17
---

## Scope

**`/` — the split console.** **Files:** `apps/site/src/pages/index.astro`. **Work:** recompose the entry as the split console: the left selection column is plan → models → tasks per month, pre-filled with a real committed plan and its measured-against model, and the right column is the readout listing (`Plan · Model priced · Effective monthly cost · Break-even tasks/month`, with the last two numeric and right-aligned, the line-number gutter, the 2px head rule and a foot line carrying the route count and the API-list multiple), with the effective-cost head stating `API list` in words; keep the ported calculator's arithmetic and its missing-route reasons exactly; serialize every control to the URL (`plan`, `models`, `tasks`) and restore from it; replace the "Top insights" paragraphs with listing rows whose value and provenance are readable without hover. The surface brief's first viewport is band + rail + split console, so **no authored figure opens `/`** — the headline is the readout listing's own first line, and any authored technical figure goes below the fold as its own section. **Done when** `/` with no query shows a committed plan rather than a placeholder, changing plan, models or task count writes the URL and a reload reconstructs the state, the five former insight statements are rows rather than paragraphs, and the browser makes no `.json` or API request.

## Acceptance criteria

- [ ] Every clause of the task's **Done when** in `## Scope` holds, verified from a built preview (`bun run build` then `bun run preview`); stage acceptance: `docs/pm/M5/README.md`.

## Notes

Migrated verbatim from the pre-PM `PLAN.md` (deleted after the migration; recover with `git show d95e6ef:PLAN.md`) stage 5 task 5.6 by the PM init on 2026-09-17; the stage's boundaries and acceptance live in `docs/pm/M5/README.md`. The task has not been enriched or rewritten.

Blocked on 2026-09-18 after the second `pm-ui-verifier` pass returned `fix` with the task's single fix round already consumed, and returned to `status: todo` by `DEC-2026-09-17-011` (accepted, `docs/pm/decisions/2026-09-17-011.md`), which is the `blocked_by` value this document now carries: the remaining phone-width failures are an equal division of the table's width (62.390625 = 312/5, 68.390625 = 342/5, 72 = 432/6, 92 = 552/6) because `display: block` on the header row inside `@media (max-width: 767px)` takes the first row out of the structure `table-layout: fixed` reads its tracks from, so the tracks move to a `<colgroup>` on the table itself and the fix pass must measure rather than reason. The acceptance criteria, `pre` and the document's history are unchanged.

Resolved by `DEC-2026-09-17-009` (accepted, `docs/pm/decisions/2026-09-17-009.md`): the unlayered console block in `apps/site/src/styles/global.css` pins an explicit width per column per table instead of letting `table-layout: fixed` redistribute it, and the phone form differs by table — the readout listing keeps 44px single rows below 768px with the state mark inline and no gutter below 480px, while `.console-table--index` keeps the stacked labelled-record form. That decision settles the phone-form choice this document left open; the acceptance criteria above are unchanged, `pre` is untouched, the task stays in `docs/pm/M5/todo/`, and `blocked_by` was left `null` because the decision was already accepted.

## Session

_UI-506 has not been finished: the task document is still open in `docs/pm/M5/todo/`. This record was relocated from `ARCHITECTURE.md` on 2026-09-17._

### Stage 5 record — `/` the split console (task UI-506)

Relocated verbatim from `ARCHITECTURE.md` on 2026-09-17; a reference document holds current truth only, so this per-task record lives with the task instead.

UI-506 turns `apps/site/src/pages/index.astro` into the Stage 5 split console. The server renders the committed plan/model route table, headline index, headline figures, independent gap set-asides, and the readout prefill. The inline `console-data` payload is the only client input; the browser owns plan/model/task selection, canonical `history.replaceState` URL serialization, row cloning, and the effective-cost arithmetic without a fetch or a client-side data request. `apps/site/src/lib/readout.ts` no longer carries the unreachable `/` switch arm.

The console keeps the listing contracts from UI-505: native labelled controls, `StateCell`, `Gutter`, `SetAside`, `EmptyState`, `SourceLink`, `CostBasisChip`, readout attributes on every row, identity links, and keyboard-visible row focus. The table header is sticky beneath the status band on desktop; phone layouts collapse the header into labelled stacked cells, remove the gutter, and preserve the state mark. No-query defaults resolve to `claude-pro`, `claude-opus-4.8`, and 300 tasks; invalid URL values fall back to committed defaults, while an unknown model selection renders the filtered empty state.

The built preview at `http://localhost:4321/rack-rate/` was exercised at 1440 × 900 and 390 × 844 in the dark scheme. The default state rendered one route, 300 tasks, and the Anthropic readout; a `chatgpt-plus` selection serialized `plan`, `models`, and `tasks`, restored those values on reload, and updated the readout before paint. The 1440px document measured `scrollWidth === clientWidth === 1440`; the 390px document measured `scrollWidth === clientWidth === 390`, with a 342px listing table inside a 390px viewport. The built page emitted no `.json` or `/api` requests during reload and interaction.

`bun run build` completed 53 pages and generated the 1200 × 630 social card. `bun run check` completed with zero errors after the final source pass; the focused `bun test apps/site/src/lib/readout.test.ts` run completed with 2 passes and 0 failures. The impeccable detector reported no output for `apps/site/src/pages/index.astro`. The deferred consumers remain UI-507 through UI-513; this route does not change data, packages, URL meaning, or arithmetic.

### Session — 2026-09-18, orchestrated run, task not finished

The 1234-line `apps/site/src/pages/index.astro` was decomposed under the UI structure contract rather than edited in place. The route is now a 72-line composition over `apps/site/src/lib/console-data.ts` (`buildConsole()` and the server-side assembly), `apps/site/src/components/ConsoleSelection.astro`, `apps/site/src/components/ConsoleListing.astro`, `apps/site/src/components/ProvenanceIndexTable.astro` (one component replacing the table that was duplicated twice), and `apps/site/src/lib/console-client.ts` (`initConsole()`, the former 398-line inline script). The console listing table grammar moved from the route's page-local `<style>` into an unlayered block in `apps/site/src/styles/global.css`; it is deliberately not inside `@layer components`, because the `480–767px` branch competes with Tailwind's `sticky` utility on the same element and only unlayered author CSS keeps the precedence the scoped route style had. Every element id, `data-*` hook, template, copy string, `readoutAttributes` field and `data-label` crossed over verbatim, and `apps/site/src/lib/readout.ts` kept its removed unreachable `/` switch arm.

`bun run check` exits 0 across the union (typecheck, oxlint, markdown lint 0 errors in 165 files, `oxfmt --check` clean, `astro check` 0 errors/0 warnings/0 hints over 83 files); `bun run build` completed 53 pages with the 1200 × 630 social card; the impeccable detector reported no findings for the route; `data/derived.json` still hashes `7425a331008fe0a1281a6d4f0bf4f350987f656cd135141a1ac69ef3f2317348`.

The task does not finish. Two `pm-ui-verifier` passes returned `fix`. The first reproduced four geometry failures on the built preview — route row 36.5px at 1440 and 768px instead of 36px, 153px at 390px instead of 44px, state cell 30.59px and line gutter 61.20px at 768px instead of 20px and 40px — and an unknown `models` value rendering the default route instead of the filtered empty state. One fix round changed `apps/site/src/styles/global.css` and `apps/site/src/lib/console-client.ts`; it fixed the unknown-model path, which now yields zero rows with `No rows match the current filters.` for `models=not-a-model`, an explicit `models=`, and an all-checkboxes-cleared selection, and it left the URL unrewritten. The second pass reproduced the geometry failure and found a regression: at 360–600px the five `HEADLINE INDEX` rows keep a fixed 44px track against 220px of content, so label, value, basis, confidence and source text overlap, visible in the rebuilt 390px greyscale capture.

Root cause recorded for the follow-up task, both in `apps/site/src/styles/global.css`: `table-layout: fixed` on a full-width table distributes the leftover width proportionally across every column, so a cell-level `width` cannot pin a column — the measured scaling factor is 1.53 at 768px and 1.0085 at 1440px for every column alike; and the base rule `.console-table tbody tr, .console-table tbody tr > td { height: 36px }` also applies inside the `.console-table--index` mobile grid, holding each cell at 36px against content that needs more. A follow-up must also settle the phone form, which the documents leave open: the `/` brief says 390px keeps 44px rows with the state mark inline before the plan name, while the shared shell drop order says rows stack as labelled records at 360px. No decision record exists for that choice yet, so this task stays in `todo/` and no commit was made.

### Session — 2026-09-18, orchestrated run, still not finished (second verification failure)

One `pm-ui-implementer` pass, one `pm-ui-verifier` pass, `DEC-2026-09-17-010`, one fix round and one re-verification. The task consumed its single allowed fix round, so the re-verification's `fix` is recorded here as a blocker rather than cycled again.

The implementer pass changed `apps/site/src/styles/global.css` only — `table-layout: auto` with per-table pinned state and gutter columns, the blanket 36px height rescoped to the readout listing, the `<=360px` readout stacking branch deleted, and mobile model truncation added — and reported honestly that its single `bun run build` and every preview capture (1440 dark, 390 light, 360 dark, 480/600/768 dark) predated its final truncation edit. Its `bun run check` passed on the second run and the impeccable detector returned `[]`.

The first verification rebuilt the tree and measured 360/390/480/600/768/1440 in both schemes. Eight criteria passed: the no-query default renders Claude Pro / claude-opus-4.8 / 300 tasks, the URL round-trip writes and reconstructs `plan`/`models`/`tasks`, the five former insight statements are rows, the browser makes no `.json` or `/api` request, the 390px greyscale capture keeps every state and basis and confidence distinction, the 2px head rule and the sticky head at 32px and the flat rules and the frozen ten tokens hold, the empty-state and URL behaviour for `models=not-a-model`, `?models=` and an all-cleared selection holds, keyboard traversal reaches every control and row, and all four stage commands exit 0. Five failed: readout rows 44.5px at 360/390/480/600, 36.5px at 768 and 40.5px at 1440 against 44px and 36px; the state cell 28.6875px at 768px against 20px; the gutter 57.390625px at 768px against 40px; the headline index and headline figures row heights 40.5–160.5px at 768px and 1440px against 36px; and a 391px document inside a 360px viewport in both schemes.

`DEC-2026-09-17-010` (accepted) was written before the fix round because the measurements exposed a conflict the documents never answered: `DEC-2026-09-17-009` clause (b) requires all four readout fields present below 768px with no horizontal scroll, while DESIGN.md's shell metric gives every numeric column a 72px minimum, and at 360px inside a 312px content width both cannot hold. The record withdraws `table-layout: auto` plus cell-level `width`/`min-width`/`max-width` as a mechanism because it was measured content-derived, requires a mechanism whose used width equals its specified width, rules that the four fields outrank the numeric minimum below 768px, and states that a `<tr>` bounding rect under `border-collapse: collapse` carries half of the shared 1px rule, so the uniform +0.5px is that collapsed border and not a track defect.

The fix round changed `apps/site/src/styles/global.css` only: `table-layout: fixed` with per-table pinned columns — readout state 20 / gutter 40 / model 160 / numerics 120, index state 20 / gutter 40 / value 88 / basis 112 / confidence 100 / source 180 / retrieved 100 with the label column left flexible — desktop cells single-line with ellipsis, the mobile readout fixed at 44px with the model at 88 and the numerics at 56 and min-widths reset, and the index mobile widths reset to content-sized. Its single `bun run check` aborted on unformatted markdown in the orchestrator's own decision document; the orchestrator then formatted `docs/pm/**` and `bun run format:check` exits 0 on 318 files.

The re-verification returned `fix` a second time. Four of the five failed criteria now pass: readout rows measure exactly 44px below 768px and exactly 36px at 768px and 1440px as content heights, the headline index and headline figures rows measure 36px with a 20px state cell and a 40px gutter at 768px and 1440px and stacked labelled records with no clipping or overlap below, and the 360px document measures `scrollWidth 360` against `clientWidth 360` in both schemes. Two criteria remain failed, both below 768px and both new to this round: the state cell measured 62.390625px at 360, 68.390625px at 390, 72px at 480 and 92px at 600 against 20px at every width, and the gutter measured 72px at 480 and 92px at 600 against 28px, while correctly absent at 360/390 and 40px at 768/1440.

Root cause for the next attempt: in `table-layout: fixed` with `width: 100%`, leftover width is distributed across the columns when no column is left flexible, which inflates the pinned state cell and gutter below 768px; the 768px-and-above branches keep one flexible column and therefore hold. The next pass must leave exactly one flexible column in each mobile branch, or remove the slack outright, and must be re-measured on a rebuilt preview rather than reasoned from source.

State at the end of this session: `bun run check`, `bun test` (165 pass / 0 fail), `bun run build` (53 pages) and `bun run data:check` all exit 0, with `data/derived.json` at `7425a331008fe0a1281a6d4f0bf4f350987f656cd135141a1ac69ef3f2317348`. The task stays in `docs/pm/M5/todo/` at `status: todo` with `blocked_by: null`, and no commit was made.

### Session — 2026-09-18, orchestrated run, third pass (not finished; a new decision is required)

One `pm-ui-implementer` pass and one `pm-ui-verifier` pass — the final implementation and verification passes that DEC-2026-09-17-011 clause (d) authorizes. The implementation pass landed the table-anchored track mechanism the decision mandated; the verification pass rebuilt the tree, served the built preview at `http://localhost:4321/rack-rate/`, and measured 360, 390, 480, 600, 768 and 1440 in both schemes.

The mechanism as shipped: a `<colgroup>` on each table — six `<col>`s on the readout listing (`console-col-state`, `-gutter`, `-flex`, `-model`, `-value`, `-value`) and eight on `.console-table--index` — with the cell-level `tr > :nth-child(n)` width rules deleted; the readout gutter kept as a real cell at zero width below 480px so the track count never changes; the mobile head hidden with `position: static; visibility: collapse; height: 0` in place of the `display: block` and `position: absolute` pair that had voided the first row; and `display: block` on the mobile index table and its `tbody` for the stacked records. The impeccable detector returned no output for the three changed paths.

The rebuilt preview measured the readout listing's used tracks (`state, gutter, plan, model, cost, break-even`) as `[20, 0, 92, 88, 56, 56]` at 360, `[20, 0, 122, 88, 56, 56]` at 390, `[20, 28, 184, 88, 56, 56]` at 480, `[20, 28, 304, 88, 56, 56]` at 600, `[20, 40, 244, 160, 120, 120]` at 768 and `[20, 40, 4, 160, 120, 120]` at 1440, identically in both schemes; row content heights are exactly 44px below 768px and exactly 36px at 768 and 1440, with bounding rects of 44.5px and 36.5px carrying the collapsed shared border DEC-2026-09-17-010 clause (d) already excuses. The `.console-table--index` tracks measure `[20, 40, 64, 88, 112, 100, 180, 100]` at 768 and `[20, 40, 528, 88, 112, 100, 180, 100]` at 1440 with 36px rows. The 360px figure is the 92px flexible `Plan` column DEC-2026-09-17-011's Context predicted, and the equal-division regression signature (312/5, 342/5, 432/6, 552/6) appears at no width: the pins hold.

Ten of the eleven criteria pass. The no-query default renders Claude Pro / claude-opus-4.8 / 300 tasks; a change of plan, models and tasks writes `?plan=chatgpt-plus&models=gpt-5.6-luna%2Cgpt-5.6-sol&tasks=777` and a reload restores it; the five former insight statements are the rows `headline-largest-multiple`, `headline-cross-check`, `headline-composite`, `headline-unpriced-models` and `headline-unresolved-plans`; 24 captured requests contain no `.json` or `/api` URL; the readout geometry, the index geometry and its stacked records, the 390px greyscale retention capture, the sticky head at 32px with its 2px rule, the ten frozen `--color-*` tokens and the three empty-state cases (`models=not-a-model`, an explicit `models=`, an all-cleared selection, each zero rows with `No rows match the current filters.` and the URL unrewritten) all hold; and `bun run check`, `bun test` (165 pass / 0 fail), `bun run build` (53 pages, 1200 × 630 card) and `bun run data:check` exit 0 with `data/derived.json` still at `7425a331008fe0a1281a6d4f0bf4f350987f656cd135141a1ac69ef3f2317348`.

One criterion fails, and it is the orchestrator's own risk probe rather than a clause of this task or of the stage: with the mobile head collapsed, the first body row begins 1px below the table top at 360, 390, 480 and 600 in both schemes, in all three tables, and removing the `thead` at runtime closes the gap to 0px. No document in this repository names a top-of-table offset, every frozen metric above passes, and `visibility: collapse` is a mechanism DEC-2026-09-17-011 clause (a) sanctioned.

No fix round was run. DEC-2026-09-17-011 clause (d) records that the task's single allowed fix round was consumed by the previous session, that this pass was the last authorized one, and that a failure of it is a new blocker requiring a new decision. The task therefore stays in `docs/pm/M5/todo/` at `status: todo` with `blocked_by: DEC-2026-09-17-011` (spent), no file was moved, and no commit was made. The remediation the next decision can authorize is one line in the fenced `apps/site/src/styles/global.css`: the mobile head rule can become `display: none`, which is safe now precisely because the tracks are anchored to the tables' own `<colgroup>` and no longer read from the first row.

UI-506 closed on 2026-09-18 by `DEC-2026-09-17-012` (accepted, `docs/pm/decisions/2026-09-17-012.md`), which narrowed this task's scope rather than spend a further fix round. Two items were withdrawn from the acceptance criteria: the 1px mobile top-of-table offset above, as not a UI-506 defect, and the readout listing's flexible `Plan` column at and above the 1280px boundary — a 464px table box against 460px of pinned tracks, derived from the committed layout as 1168 less the 360px selection column and its 32px gap less the 288px set-aside column and its 24px gap, so the committed plan name renders clipped at desktop — which is carried into the design overhaul as a recorded known gap rather than hidden. Every remaining criterion is met: the four Done-when clauses hold below 1280px, the stage acceptance in `docs/pm/M5/README.md` holds, and the frozen metrics of `DEC-2026-09-17-009`, `-010` and `-011` measure green in both schemes at every viewport of the matrix.

## Session

Closed by narrowing scope under the owner's in-conversation approval, recorded as DEC-2026-09-17-012 (accepted). One pm-ui-implementer pass and one pm-ui-verifier pass, the last authorized by DEC-2026-09-17-011 clause (d); no fix round was spent.

Implementation (files_changed): apps/site/src/components/ConsoleListing.astro, apps/site/src/components/ProvenanceIndexTable.astro and apps/site/src/styles/global.css. A `<colgroup>` on each table with exactly one flexible column (six cols readout, eight cols index), the cell-level `tr > :nth-child(n)` width rules deleted, the readout gutter kept as a constant real cell at zero width below 480px so no row's cells ever shift a column left, and the mobile head hidden with `position: static; visibility: collapse; height: 0` in place of the `display: block` and `position: absolute` pair that had taken the first row out of the structure `table-layout: fixed` read its tracks from.

Verification, from a rebuilt preview at http://localhost:4321/rack-rate/ in both schemes, never astro dev. Readout used tracks [state, gutter, plan, model, cost, break-even]: [20, 0, 92, 88, 56, 56] at 360, [20, 0, 122, 88, 56, 56] at 390, [20, 28, 184, 88, 56, 56] at 480, [20, 28, 304, 88, 56, 56] at 600, [20, 40, 244, 160, 120, 120] at 768, [20, 40, 4, 160, 120, 120] at 1440. Row content heights exactly 44px below 768px and exactly 36px at 768 and 1440, the 44.5px and 36.5px bounding rects being the collapsed shared border DEC-2026-09-17-010 clause (d) already excuses. Index tracks [20, 40, 64, 88, 112, 100, 180, 100] at 768 and [20, 40, 528, 88, 112, 100, 180, 100] at 1440 with 36px rows. scrollWidth === clientWidth at 360, 390, 480 and 600. The equal-division regression signature (312/5, 342/5, 432/6, 552/6) appears at no width: the pins hold. Ten of eleven criteria green, including the no-query default (Claude Pro / claude-opus-4.8 / 300 tasks), the plan/models/tasks URL round trip, the five former insight statements as rows, zero .json or /api requests of 24 captured, the 390px greyscale retention capture, the sticky head at 32px with its 2px rule, exactly the frozen ten --color-* tokens, the three empty-state cases, and keyboard traversal.

Withdrawn from the acceptance criteria by DEC-2026-09-17-012 rather than fixed: the 1px mobile top-of-table offset left by the collapsed head at 360/390/480/600 in both schemes, which no document names and which is a collapsed-boundary remnant of the class DEC-2026-09-17-010 clause (d) classifies as not a track defect; and the readout listing's flexible `Plan` column at and above the 1280px boundary, where the table's box is 464px (1168 workspace less the 360px selection column, its 32px gap, the 288px set-aside column and its 24px gap) against 460px of pinned tracks, leaving 4px and clipping the committed plan name at desktop. The verifier independently measured the same 464px box and confirmed the 4px track is the real flexible cell, so the desktop column is carried into the design overhaul as a recorded known gap, not hidden.

Gate at closure: bun run check, bun test (165 pass / 0 fail), bun run build (53 pages, 1200x630 card) and bun run data:check all exit 0, with data/derived.json still at 7425a331008fe0a1281a6d4f0bf4f350987f656cd135141a1ac69ef3f2317348. The impeccable detector returned no output for the three changed paths. The working tree was formatted with bun run format and the full gate re-run green before this commit.
