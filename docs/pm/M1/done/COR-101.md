---
id: COR-101
title: Workspace skeleton
description: "Root package.json with the three-workspace layout and the three package manifests."
status: done
milestone: M1
pre: []
kind: chore
blocked_by: null
created: 2026-09-14
completed: 2026-09-14
---

## Scope

Workspace skeleton: root `package.json` with `workspaces: ["packages/*", "apps/*"]`, `packages/core`, `packages/data-cli`, `apps/site` each with their own `package.json`. _(root + package manifests written)_

## Acceptance criteria

- [x] The requirement text in `## Scope` is satisfied; the landing evidence is the stage session record in `## Session` and the stage acceptance at `docs/pm/M1/README.md`.

## Notes

Split verbatim from the frozen pre-PM stage record `docs/history/stages-1-2.md` §`Stage 1 — Initialization` task 1.1 by the PM history parse on 2026-09-17. The canonical id is `COR-101`; the stage's label `1.1` is its own numbering in force at the time, not a task name. The stage's acceptance and handover contract live at `docs/pm/M1/README.md`. The task text has not been enriched or rewritten.

## Session

The stage logged this work inside its stage-level entry `### 2026-09-14 — Initialization (bootstrap, before Stage 1 was executed by an agent)`; that entry is preserved verbatim in `docs/pm/M1/README.md` § Stage session log and in `docs/history/stages-1-2.md`. No per-task session entry exists.
