---
id: COR-102
title: Root tsconfig with no project references
description: "Root tsconfig.json plus one per package, with the single-root, no-project-references choice decided and recorded; bun run typecheck must typecheck all three trees."
status: done
milestone: M1
pre: []
kind: chore
blocked_by: null
created: 2026-09-14
completed: 2026-09-14
---
## Scope

Root `tsconfig.json` (project references into the three packages) and one `tsconfig.json` per package. Decide and record: single root `tsconfig` vs references. Requirement: `bun run typecheck` typechecks all three trees.

## Acceptance criteria

- [x] The requirement text in `## Scope` is satisfied; the landing evidence is the stage session record in `## Session` and the stage acceptance at `docs/pm/M1/README.md`.

## Notes

Split verbatim from the frozen pre-PM stage record `docs/history/stages-1-2.md` §`Stage 1 — Initialization` task 1.2 by the PM history parse on 2026-09-17. The canonical id is `COR-102`; the stage's label `1.2` is its own numbering in force at the time, not a task name. The stage's acceptance and handover contract live at `docs/pm/M1/README.md`. The task text has not been enriched or rewritten.

## Session

The stage logged this work inside its stage-level entry `### 2026-09-14 — Stage 1 executed (initialization complete)`; that entry is preserved verbatim in `docs/pm/M1/README.md` § Stage session log and in `docs/history/stages-1-2.md`. No per-task session entry exists.
