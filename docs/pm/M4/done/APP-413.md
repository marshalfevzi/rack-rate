---
id: APP-413
title: "`/explore` — the chart builder"
description: "`/explore` chart builder letting the visitor choose y and x metrics, chart type, filters, axis scale and frontier shading, plus composite weight sliders recomputing the composite client-side, every setting encoded in the URL."
status: done
milestone: M4
pre: []
kind: feature
blocked_by: null
created: 2026-09-14
completed: 2026-09-14
---
## Scope

`/explore` — the chart builder, and the answer to "the graphs are
  bad and there are no custom graphs": the visitor chooses the y metric (any
  benchmark score, composite `T`, or token-ratio metric), the x metric (API-list
  $/task, plan-adjusted $/task, tokens/task, steps/task, or a benchmark score
  for a head-to-head scatter), the chart type, the filters (vendor, effort
  level, score floor, ignored models/plans), a log/linear axis toggle, and
  whether the Pareto frontier and dominated-region shading are drawn. Plus the
  composite weight sliders with presets, recomputing the composite client-side
  from shipped per-benchmark z-scores — no refetch. Every setting is encoded in
  the URL so a configuration is shareable, and it persists as a preference.

## Acceptance criteria

- [x] The requirement text in `## Scope` is satisfied; the landing evidence is the stage session record in `## Session` and the stage acceptance at `docs/pm/M4/README.md`.

## Notes

Split verbatim from the frozen pre-PM stage record `docs/history/stages-4.md` §`Stage 4 — Frontend build (charts and insight pages)` task 4.13 by the PM history parse on 2026-09-17. The canonical id is `APP-413`; the stage's label `4.13` is its own numbering in force at the time, not a task name. The stage's acceptance and handover contract live at `docs/pm/M4/README.md`. The task text has not been enriched or rewritten.

## Session

The stage logged this work inside its stage-level entry `### 2026-09-14 — Stage 4.4–4.15: the remaining charts, the insight pages, the mobile pass`; that entry is preserved verbatim in `docs/pm/M4/README.md` § Stage session log and in `docs/history/stages-4.md`. No per-task session entry exists.
