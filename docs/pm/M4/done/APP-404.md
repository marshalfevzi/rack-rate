---
id: APP-404
title: Model × benchmark heatmap
description: "Model × benchmark heatmap with a diverging visualMap centred on 0 and hatched neutral cells for not-evaluated entries rather than a low score."
status: done
milestone: M4
pre: []
kind: feature
blocked_by: null
created: 2026-09-14
completed: 2026-09-14
---
## Scope

**Model × benchmark heatmap** — diverging `visualMap` centred on 0,
  hatched neutral cells for "not evaluated", never a low score.

## Acceptance criteria

- [x] The requirement text in `## Scope` is satisfied; the landing evidence is the stage session record in `## Session` and the stage acceptance at `docs/pm/M4/README.md`.

## Notes

Split verbatim from the frozen pre-PM stage record `docs/history/stages-4.md` §`Stage 4 — Frontend build (charts and insight pages)` task 4.4 by the PM history parse on 2026-09-17. The canonical id is `APP-404`; the stage's label `4.4` is its own numbering in force at the time, not a task name. The stage's acceptance and handover contract live at `docs/pm/M4/README.md`. The task text has not been enriched or rewritten.

## Session

The stage logged this work inside its stage-level entry `### 2026-09-14 — Stage 4.4–4.15: the remaining charts, the insight pages, the mobile pass`; that entry is preserved verbatim in `docs/pm/M4/README.md` § Stage session log and in `docs/history/stages-4.md`. No per-task session entry exists.
